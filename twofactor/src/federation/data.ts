/**
 * Federated data layer for the 2FA micro-frontend. Plain async functions the
 * host can await in a loader or a widget can call directly.
 */
export { verifyCode, DEMO_2FA_CODE } from "@/mock/verify";
